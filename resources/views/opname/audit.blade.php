<!doctype html>
<html lang="id">
<head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><title>Audit Log</title>@vite(['resources/css/app.css','resources/js/app.js'])</head>
<body><main class="form-page"><a class="text-link" href="{{ route('dashboard') }}">← Dashboard</a><div class="form-card wide"><p class="eyebrow">KONTROL ADMIN</p><h1>Audit log stock opname</h1><p class="intro-copy">Riwayat perubahan sesi, hitungan, verifikasi, approval, dan override.</p><table><thead><tr><th>Waktu</th><th>Pengguna</th><th>Aksi</th><th>Objek</th><th>Perubahan</th></tr></thead><tbody>@forelse($logs as $log)<tr><td>{{ $log->created_at?->format('d M Y H:i:s') }}</td><td>{{ $log->user?->name ?? 'Sistem' }}</td><td>{{ $log->action }}</td><td>{{ class_basename($log->auditable_type) }} #{{ $log->auditable_id }}</td><td><small>{{ json_encode($log->new_values, JSON_UNESCAPED_UNICODE) }}</small></td></tr>@empty<tr><td colspan="5">Belum ada aktivitas.</td></tr>@endforelse</tbody></table>{{ $logs->links() }}</div></main></body></html>
