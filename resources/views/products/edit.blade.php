<!doctype html>
<html lang="id">
<head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><title>Ubah Produk</title>@vite(['resources/css/app.css','resources/js/app.js'])</head>
<body><main class="form-page"><a class="text-link" href="{{ route('products.index') }}">← Produk &amp; batch</a><div class="form-card"><p class="eyebrow">MASTER DATA</p><h1>Ubah produk</h1>@if($errors->any())<div class="error-message"><ul>@foreach($errors->all() as $error)<li>{{ $error }}</li>@endforeach</ul></div>@endif<form method="POST" action="{{ route('products.update', $product) }}" enctype="multipart/form-data">@csrf @method('PUT')<label>Nama produk<input name="name" value="{{ old('name', $product->name) }}" required></label>@if($product->image_path)<img src="{{ Storage::url($product->image_path) }}" alt="{{ $product->name }}" style="width:140px;height:140px;object-fit:cover;border-radius:8px">@endif<label class="image-upload" data-image-upload>Ganti gambar produk<input type="file" name="image" accept="image/jpeg,image/png,image/webp" capture="environment"><span class="image-actions"><button type="button" data-camera-button>Ambil gambar</button></span><small>Ambil foto produk menggunakan kamera. Kosongkan jika gambar tidak diubah. Maksimal 5 MB.</small></label><label>SKU<input name="sku" value="{{ old('sku', $product->sku) }}" required></label><label>Barcode<input name="barcode" value="{{ old('barcode', $product->barcode) }}" required></label><label>Kategori<select name="product_category_id" required>@foreach($categories as $category)<option value="{{ $category->id }}" @selected(old('product_category_id', $product->product_category_id) == $category->id)>{{ $category->name }}</option>@endforeach</select></label><label>Satuan<input name="unit" value="{{ old('unit', $product->unit) }}" required></label><label>Konversi satuan<input type="number" name="unit_conversion" min="1" value="{{ old('unit_conversion', $product->unit_conversion) }}" required></label><label><input type="checkbox" name="is_active" value="1" @checked(old('is_active', $product->is_active))> Produk aktif</label><button class="primary-button" type="submit">Simpan perubahan</button></form></div></main></body>
</html>
