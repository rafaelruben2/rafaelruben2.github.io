<!doctype html>
<html lang="id">
<head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><title>Produk &amp; Batch</title>@vite(['resources/css/app.css','resources/js/app.js'])</head>
<body><div class="app-shell">@include('partials.sidebar')<main class="main-content">@include('partials.header')<section class="form-page"><div class="form-card wide"><div class="panel-heading"><div><p class="eyebrow">MASTER DATA</p><h1>Produk &amp; batch</h1></div><a class="primary-button" href="{{ route('products.create') }}">+ Tambah produk</a></div>@if(session('success'))<p class="success-message">{{ session('success') }}</p>@endif<table><thead><tr><th>Gambar</th><th>SKU</th><th>Nama</th><th>Kategori</th><th>Barcode</th><th>Status</th><th>Aksi</th></tr></thead><tbody>@forelse($products as $product)<tr><td>@if($product->image_path)<img src="{{ Storage::url($product->image_path) }}" alt="{{ $product->name }}" style="width:56px;height:56px;object-fit:cover;border-radius:6px">@else<span>-</span>@endif</td><td>{{ $product->sku }}</td><td>{{ $product->name }}</td><td>{{ $product->category?->name ?? '-' }}</td><td>{{ $product->barcode }}</td><td>{{ $product->is_active ? 'Aktif' : 'Nonaktif' }}</td><td><a class="text-link" href="{{ route('products.edit', $product) }}">Ubah</a><form class="inline-form" method="POST" action="{{ route('products.destroy', $product) }}">@csrf @method('DELETE')<button type="submit">Hapus</button></form></td></tr>@empty<tr><td colspan="7">Belum ada produk. Tambahkan produk pertama.</td></tr>@endforelse</tbody></table></div></section></main></div></body>
</html>
